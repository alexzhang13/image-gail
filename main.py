import numpy as numpy
import torch 
import torch.nn as nn 
import torchvision

from models.gail import Disciminator, Policy

# add variables here for parser later
seed = 7

# init environment

# init random seeding
np.random.seed(seed)
torch.manual_seed(seed)

def update():
    pass

def main_loop():
    pass